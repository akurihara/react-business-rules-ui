import React, { Component, PropTypes } from 'react';

class Rule extends Component {

  render() {
    return (
      <div className="rule">
        Rule
      </div>
    );
  }
}

Rule.propTypes = {

};

export default Rule;
